<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_voice_effect"
    android:layout_width="136dp"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardBackgroundColor="@color/card_bg"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardPreventCornerOverlap="true">

    <LinearLayout
        android:id="@+id/layout_effect_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Top Row: Icon Container + Category Tag Badge -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp">

            <FrameLayout
                android:id="@+id/layout_icon_container"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_alignParentStart="true"
                android:background="@drawable/bg_icon_glow">

                <ImageView
                    android:id="@+id/iv_effect_icon"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:contentDescription="Voice Effect Icon"
                    android:src="@drawable/ic_mic_24"
                    android:tint="@color/accent_blue" />
            </FrameLayout>

            <TextView
                android:id="@+id/tv_effect_tag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="@drawable/bg_status_pill"
                android:fontFamily="@font/mitr_bold"
                android:paddingHorizontal="6dp"
                android:paddingVertical="2dp"
                android:textColor="@color/tag_rx_text"
                android:textSize="9sp"
                tools:text="ACTIVE" />

        </RelativeLayout>

        <!-- Effect Name -->
        <TextView
            android:id="@+id/tv_effect_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/mitr_bold"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            tools:text="Robotic" />

        <!-- Effect Description -->
        <TextView
            android:id="@+id/tv_effect_desc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:fontFamily="@font/mitr_regular"
            android:maxLines="1"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            tools:text="Ring Modulator" />

        <!-- DSP Architecture Info Tag -->
        <TextView
            android:id="@+id/tv_effect_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:background="@drawable/bg_frequency_chip"
            android:ellipsize="end"
            android:fontFamily="@font/mitr_light"
            android:maxLines="1"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:textColor="@color/text_muted"
            android:textSize="9sp"
            tools:text="DSP: 85Hz SINE" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
