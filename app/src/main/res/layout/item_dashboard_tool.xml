<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_tool"
    android:layout_width="134dp"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardBackgroundColor="@color/card_bg"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardPreventCornerOverlap="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Top Row: Icon Container + Category Tag Badge -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp">

            <FrameLayout
                android:id="@+id/layout_icon_container"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_alignParentStart="true"
                android:background="@drawable/bg_icon_glow">

                <ImageView
                    android:id="@+id/iv_tool_icon"
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:contentDescription="Tool Icon"
                    android:src="@drawable/ic_preset_aircraft_24"
                    android:tint="@color/accent_blue" />
            </FrameLayout>

            <TextView
                android:id="@+id/tv_tool_tag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="@drawable/bg_status_pill"
                android:fontFamily="@font/mitr_bold"
                android:paddingHorizontal="6dp"
                android:paddingVertical="2dp"
                android:textColor="@color/tag_rx_text"
                android:textSize="9sp"
                tools:text="RX" />

        </RelativeLayout>

        <!-- Tool Name -->
        <TextView
            android:id="@+id/tv_tool_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/mitr_bold"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            tools:text="ADS-B Radar" />

        <!-- Frequency Information -->
        <TextView
            android:id="@+id/tv_tool_frequency"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:fontFamily="@font/mitr_regular"
            android:maxLines="1"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            tools:text="1090 MHz" />

        <!-- Bandwidth / Modulation Info -->
        <TextView
            android:id="@+id/tv_tool_bandwidth"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:background="@drawable/bg_frequency_chip"
            android:ellipsize="end"
            android:fontFamily="@font/mitr_light"
            android:maxLines="1"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:textColor="@color/text_muted"
            android:textSize="9sp"
            tools:text="BW: 2.0 MHz" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
